# IntelliCash — Full-system QA report

**Date:** 19-20 September 2026 · **Scope:** API (`apps/api`), web console (`apps/web`), Android app (`intellicash_mobile`) · **Method:** black-box scenarios written independently of the code under test, run against a dedicated QA database and API, then unit/regression tests added for every fix.

A workflow was counted as passing only when the screen (or API response), the database row, the arithmetic, persistence after reload and every dependent screen all agreed.

---

## A. Test environment and accounts

| Item | Value |
|---|---|
| QA API | `http://localhost:4100/api/v1`, SQLite `apps/api/prisma/qa.db` (separate from the dev and test databases, so QA runs never reseed or sign out a developer's session) |
| Web console | Next.js dev server on `:3160`, pointed at the QA API; driven in a real browser at desktop and phone width |
| Android | Emulator `sdk_gphone16k_x86_64` (Pixel-class, 1080×2424, 420 dpi), first a debug build then a profile (AOT) build of 2.6.1+22, `IC_BASE_URL=http://10.0.2.2:4100/api/v1` (restored to the production address afterwards) |
| Harness | `qa/lib.mjs` (check registry + API client + direct DB access), `qa/01…04-*.mjs`; every check stores **expected** (computed from the script's own postings or the documented rules, never read back from the code under test) and **actual** in `qa/results.json` |
| Demo accounts (password `IntellicashDemo#2026`) | `admin@`, `partner@`, `lender@`, `group@`, `member@`, `agent@`, `readonly@intellicash.co.ke` |
| QA-created logins | Group logins `QA Calc Login` (phone in `qa/world.json`, password `QaCalcGroup#2026-x`) and `QaIntegrity#2026-x` groups, created through the API and verified in the database |

All QA credentials belong to a throw-away database. None were created on production.

## B. Workflows tested

| Area | Scenario | Checks |
|---|---|---|
| Accounts, groups, members | `01-members-and-logins` — self sign-up creates a group; admin-created groups/logins; six members with and without phones; member with **no** login; member **with** login; login-first then join request; suspend / close / re-create a login | 59 |
| Financial calculations | `02-calculations` — zero state, savings, social fund, fines, loans (interest, partial, over-payment, cap at term, oldest-first), a settled loan as time passes, fund guards, retries and concurrency, full share-out | 51 |
| Permissions | `03-permissions` — 8 accounts × 14 operations × own/foreign group, expectation computed from the role table and each account's scope read from the database; anonymous and forged sessions | 200 |
| Integrity | `04-integrity` — mismatched type/direction/fund, foreign fund/member/meeting, largest storable amount and overflow, malformed and oversized input, atomic batches, concurrent loans, idempotent replay, hostile text | 32 |
| Phone app | See section H-2 (screen-by-screen on the emulator) | — |

**Result: 343 scripted checks, 0 failing after fixes** (11 failed before the fixes described below). Unit / API regression: see section G.

## C. Issue log

Severity: **High** = wrong money or lost money integrity · **Medium** = wrong figure shown, or a user-visible fault · **Low** = polish/robustness.

| ID | Sev | Feature | What was wrong (steps → expected → actual) | Root cause | Fix | Files | Regression tests | Status |
|---|---|---|---|---|---|---|---|---|
| QA-001 | **High** | Share-out / repayments across several loans | A member owed 550.00 across two loans (250.00 + 300.00). Share-out netted the whole 550.00 in one repayment row. → expected nothing owed afterwards → actual the passbook showed 300.00 still owed on the newer loan, which the **next** share-out would collect again (double charge). | A repayment row points at one loan; per-loan arithmetic clamped the surplus to zero, so the overflow into the next loan vanished. | New pure `memberLoanPosition` replays a member's repayments in order, oldest loan first, at what each loan owed **that day**; surplus rolls to the next loan; true over-payment is reported once. | `domain/loan-math.ts`, `routes/groups.ts` | `tests/loan-math.test.ts` (26), `loan-projection.test.ts` (+2), `share-out-multi-loan.test.ts` (3), QA C7.11/C7.12 | Fixed |
| QA-002 | **High** | Interest on a settled loan | Loan of 1,000.00 at 10 %/month repaid in full (1,200.00) in month two. A month later the passbook showed **100.00 owed** on a loan whose own status said REPAID, while the share-out ignored it (three screens, three answers). | Interest was always computed to *today*; nothing froze it at settlement. | The replay marks the day a loan is covered and stops interest there. | `domain/loan-math.ts` | `loan-math.test.ts` (settled cases), `loan-projection.test.ts`, QA C4b | Fixed |
| QA-003 | Medium | One question, four answers | Passbook, meeting SMS, share-out and the partner report each worked out "what a member owes" separately. | Duplicated logic. | Single loader `services/loan-position-service.ts` used by all four. | `services/*`, `routes/groups.ts` | covered by QA-001/002 tests + `programme-performance-report.test.ts` | Fixed |
| QA-004 | Medium | Portfolio "repayment rate" | `/analytics/portfolio` returned the constant **91** to partners. | Hard-coded placeholder. | Computed from loans that have fallen due (or been settled); `null` (not 0) when nothing has fallen due. Type is now `number \| null`. | `routes/analytics.ts`, `domain/loan-math.ts`, `packages/shared` | `loan-math.test.ts` (5) | Fixed |
| QA-005 | **High** | Direct ledger route | `POST /groups/:id/ledger` accepted a loan "disbursement" as money **in** (creating a phantom loan the fund never paid), a share purchase as money **out**, a social contribution into the loan fund, a welfare expense out of the loan fund. | Type, direction and fund were chosen independently by the caller. | `appendLedgerEntry` now enforces the type→direction→fund rule for every caller. | `routes/groups.ts` | `tests/ledger-integrity.test.ts` (8), QA I1 | Fixed |
| QA-006 | Medium | Very large amounts | An amount over 21,474,836.47 (or a fund pushed past it) returned **HTTP 500 "Something went wrong on our side"**. | 32-bit integer column; no bound or friendly refusal. | Amount ceilings on the money routes, `FUND_BALANCE_LIMIT` check, and a central mapping of any "integer does not fit" database error to a 400 with a plain sentence. Nothing is half-saved. | `domain/money.ts`, `lib/http.ts`, `routes/groups.ts` | `ledger-integrity.test.ts`, QA I2 | Fixed |
| QA-007 | Low | Text length | A 5,000-character member name was stored. | No maximum on free-text fields. | Sensible caps on group, member, meeting and ledger text. | `routes/groups.ts` | QA I5.03 | Fixed |
| QA-008 | Low | Validation wording | "Amount cents must be at most 2147483647." and custom sentences glued to the field name. | Message assembly. | Money fields read "Amount"; a written sentence is shown as written. | `lib/http.ts` | `friendly-errors.test.ts` (+2) | Fixed |
| QA-009 | Low | Share-out rounding | Pro-rata used floating point (`pool × shares / total`); above ~9×10¹⁵ it can be a cent out. | JS number precision. | Exact BigInt arithmetic. | `domain/share-out.ts` | `share-out-maths.test.ts` (5) | Fixed |
| QA-010 | Medium | Partner report | Groups that record meetings on the phone showed **0 meetings held, 0 % attendance**. | Only meetings formally opened on the server counted; phone-recorded meetings stay SCHEDULED. | A meeting counts as held if opened **or** anything (attendance, money) was recorded in it. | `services/programme-performance-report.ts` | `programme-performance-report.test.ts` (+1) | Fixed |
| QA-011 | Medium | Test suite | 17 visit-attachment tests failed on any machine with under 15 % free disk (the storage guard's real threshold). | Tests depended on the host. | The guard is stubbed in those tests; its thresholds keep their own disk-free tests. | `tests/visit-attachments.test.ts` | — | Fixed |
| QA-012 | Medium | Build tooling | Android build failed with only "25.0.3" after Android Studio's bundled JDK moved to 25 (Gradle 8.12 needs ≤ 23). | Toolchain drift. | `flutter config --jdk-dir` → JDK 17 (documented). | (developer machine) | — | Fixed |
| QA-013 | **Critical** | Field-visit scorecards never reached the server | An agent scored a group (46 questions, 38 of 92 - Fair), pressed Finish; the visit arrived, the **assessment never did**. The server log showed `PUT /visits/:id/assessment` -> **404 TEMPLATE_SNAPSHOT_NOT_FOUND** on every retry, and the phone (which deliberately swallows scorecard failures so they cannot lose a visit) retried for ever in silence. Unit tests passed because they fetched the real snapshot id from the database. | `GET /assessment-templates/current` never said what the snapshot's id was, so the phone fell back to the **template's** id; a snapshot has its own id. | Server now returns `snapshotId`; and, so phones already in the field heal with **no app update**, a submission may name either the snapshot or its template (a template has exactly one snapshot). An id that is neither is still refused. Verified on the emulator: after the fix the pending scorecard synced by itself (38/92, 41.3 %, Fair, 29 answers, real snapshot id). | `services/visit-assessment-service.ts` | `tests/visit-assessments.test.ts` (+3, 19 pass) | Fixed (deploy to take effect) |

### Decisions recorded (found, deliberately not changed)

* **Idempotent replay is silent.** Re-sending a ledger entry with the same `clientRequestId` but a different amount returns the *original* entry and moves no money. A hard 409 would jam a phone's whole sync batch on one edited retry. The response shows what was actually recorded.
* **A share-out entry is a legitimate phone-sync type**, so an authorised ledger writer can post one; it re-baselines the share-out cycle (documented behaviour of the offline share-out).
* **No reversal / void for ledger entries** (append-only by design). A mistaken entry needs an offsetting entry; a first-class "correction" workflow is a product decision, not a defect.
* **Over-payment is not refunded automatically at share-out**; it is reported per loan (`overpaidCents`) and is absorbed into the pool. Groups that want refunds need a rule.
* **Maximum single figure is KES 21,474,836.47** (32-bit cents). Enforced with a clear message; widening it needs a schema migration.

## D. Calculation verification (independent)

Every expected figure below was worked out by hand or by the script from its own postings, then compared with the API, the passbook, the reports and the database.

| Topic | Rule verified | Cases |
|---|---|---|
| Savings, social fund, fines | Fund = Σ credits − Σ debits; member totals add up; fines count in "total paid in" | zero, one, many, one cent, two entries by one member |
| Loan interest | Flat monthly on the **original** principal, whole elapsed 30-day months (floor), capped at the term | 29 days → 0; 65 days → 2 months (400.00 on 2,000.00); 200 days capped at 3 months (150.00 on 500.00) |
| Repayments | Oldest loan first; partial; exact; over-payment reported not negative | 400.00 → older owes 250.00, newer still 300.00; 3,000.00 against 2,400.00 → 600.00 over, loan closed |
| Settled loan | Interest stops the day it is paid off | paid 1,200.00 at month 2 → still 0 a month later |
| Loan guards | Loan larger than the fund refused with the shortfall; zero, negative, fractional, text amounts refused | 1 cent over → `INSUFFICIENT_LOAN_FUND` shortfall 1 |
| Welfare | Spend from the social fund; cannot exceed it | one cent over → refused, fund unchanged |
| Share-out | Pool × member shares ÷ all shares (floor, exact), last member takes the remainder; welfare **remainder** split equally; loans netted at principal + interest owed today; cash conserved | payouts sum to the pool; net = payout + welfare − loans; a second request moves nothing |
| Concurrency & retries | Same request id never doubles; 6 parallel identical requests → 1 row; 8 parallel different → 8; 6 parallel 300.00 loans on 1,000.00 → exactly 3 | fund equals Σ ledger (no lost update) |
| Extremes | Largest storable amount accepted; +1 cent refused politely; 1e21 and text refused; 251-entry batch refused whole; a bad entry rolls back the good ones | — |

## E. Authentication and member model — confirmed

1. A **member exists on the server without any login** (name-only members, and members with a phone but no login); money can be recorded for them and the group reads their passbook. *(S2, 11 checks)*
2. A **member with a login is the same record** — creating a login attaches to the existing member row; no duplicate is created; the pre-login history (share bought before the login existed) is visible to the member; a member cannot read another member's passbook; a second login for the same member and a second member on the same phone in a group are refused. *(S3, 10 checks)*
3. **Login first, member later** — self sign-up creates a login with no member; a join request approved against a phone that is already on the roster requires explicit confirmation and attaches to the **existing** member; a person with no roster entry gets exactly one new member; approving twice is refused. *(S4, 11 checks)*
4. **Removing a login never removes the member** — suspending blocks sign-in with a clear reason; closing strips the login's identity and detaches it while the member row, every transaction and the group's view are unchanged; the old session dies; a fresh login can be created for the same member and reads the same history; still one member row. *(S5, 12 checks)*

## F. Backend verification

* Every UI/API-created record in scenarios 1–4 was re-read from the database (member, membership link, ledger row, loan row, fund balance) and compared, not only echoed back.
* Fund balance = Σ ledger entries after every concurrent scenario.
* Permissions matrix: 200 role × operation × scope cells match the role table; 404 (not 403) is returned for a group that is not yours; anonymous and forged sessions get 401.

## G. Regression

Final run, 20 September 2026, after every fix in this report.

| Suite | Result |
|---|---|
| API (vitest) | **82 files, 835 tests, all pass** (944 s). An earlier full run had 4 files fail for reasons that were not product faults - `visit-attachments` (host disk < 15 %, QA-011), `share-out-multi-loan` (a test asserted against an empty cycle, corrected), `admin-access` / `admin-sms-service` (30 s hook timeouts while the host was saturated) - each re-run green in isolation and green in the final run. |
| API type-check (`tsc --noEmit`, includes tests) | clean |
| Web console (vitest, 22 files) | **172 tests pass**; type-check clean |
| Phone app (`flutter test`) | **458 pass, 2 skipped, 0 fail**; `dart analyze lib test` clean. Two tests were failing before this run and were fixed: the project's translation-coverage test (it caught five hard-coded English strings written during this audit) and a stale "notify only when something synced" test (the code documents that it notifies after every run). |
| QA scenarios (`qa/01-04`, real HTTP + database) | **343 checks, 0 failing**, re-run against the restarted server with the scorecard fix |
| On-device (profile build, x86_64 emulator) | every flow in H-2 / H-2b / H-2c, records verified in the QA database |

## H. Phone app (Android) — screen-by-screen on the emulator

Method: build of the app against the QA API, driven through adb (real taps and typing), each screen read from the accessibility tree and from a screenshot, every record then checked in the QA database. Offline behaviour was tested with the phone's network switched off and on, with the **server stopped and restarted**, and with a forced restart.

Build note: the first pass used a Flutter *debug* build (JIT). On a heavily loaded host it froze on the visit screen's location capture and the emulator raised "Intelli-Cash isn't responding". That is a debug-build / overloaded-emulator artefact, not an app fault: the same screens run on a *profile* (AOT, release-like) build captured the location in seconds (`-0.53000, 37.45000 - accurate to 5 m`) and every later test used it. (A true release build refuses to compile against the QA address, which is the guard working as intended.)

### H-1. Screens and controls exercised

Welcome · language chooser (English/Kiswahili switch live, drafts marked) · Create account (Group / Just me / Field agent choice; empty, invalid and mismatched-password validation; show/hide password) · Load my group · Dashboard · Members (empty state, search hit / miss / partial, add-member sheet, member detail, edit member, requests-to-join badge) · Join requests (Waiting / Answered; approve a phone already on the roster → explicit "same person?" confirmation; approve a newcomer; both verified on the server) · More (every entry) · Group settings wizard (Basics → Savings → Loans → Schedule, multi-day chips, save) · Meeting security (3-key switch, role dropdown with all six roles, digital champion picker + confirm) · Unlock meeting (offline banner, first-time PIN, PIN rules, wrong PIN, key-turn, all three keys) · Attendance (toggle on/off, live percentage) · Meeting hub (all tiles) · Buy shares (member picker, stepper, total, payment method) · Social fund · Record fine (three validation messages, decimal amount, reason list) · Disburse loan (member eligibility, above-limit and above-fund refusals, valid loan) · Repayment (loan picker, over-payment refused, partial) · Share records · Loans list and detail · Cloud account · Online records · Voting / Welfare (offline messages).

### H-2. Phone issues found

| ID | Sev | What happened | Fix (mobile unless stated) | Status |
|---|---|---|---|---|
| M-01 | Medium | Add-member accepted "12345" as a phone number; the server then stored it verbatim as the member's identity. | Phone validates (same rule as sign-up); server: create refuses unusable numbers, sync stores such a member without a number; edit validates; champion picker hides unusable numbers. Tests: `member-sync.test.ts` (+2), phone rule. | Fixed |
| M-02 | Low | "Passwords don't match" stayed on screen after the passwords were made to match. | Form re-validates as the person types. | Fixed |
| M-03 | Medium | A member the server admitted (approved join request, member added on the web) never appeared on the phone, so they could not be marked present or take part in a meeting. | Sync now also brings down server members: add-only, matched only by canonical phone (or name for two people with no number), never edits or removes. Members list reloads after a sync and on pull-to-refresh. Tests: 3 new in `auto_link_sync_test.dart`. | Fixed |
| M-04 | Medium | The phone quoted 10 % interest while the server (no loan rules of its own) charged 0 %, so phone and console disagreed about the same loan. | On sync, when the server has no loan rules and the phone uses flat monthly interest, the phone gives the server its rate and term. Never overwrites rules that exist. Test added. | Fixed |
| M-05 | Medium | Social fund could be ticked "Paid KSh 0.00" when the amount was unset; that entry can never sync (server refuses zero) and would keep the meeting on "waiting to back up" for ever. | Refused with an explanation on the screen; sync never sends a zero-value entry. | Fixed |
| M-06 | Low | Dashboard showed a fine of 50.50 as "KSh 51" while the meeting summary showed 50.50. | Compact money keeps real cents (`KSh 50.50`), still `KSh 2,500` for whole shillings. Tests added. | Fixed |
| M-07 | Medium | After starting with no signal, welfare / cycles / group rules / payment providers told a signed-in treasurer to "choose your group under Cloud Account" and never recovered by themselves. | Signal-returned sync re-loads the connection and group; those four screens retry when the group appears and have a Try-again button; clearer wording. | Fixed |
| M-08 | Low | More → Cloud Account said "Connected · 0 members" for a group with members. | Count shown only once the roster is loaded. | Fixed |
| M-09 | Low | Add-member button had no name for a screen reader; the requests badge replaced the button's name with "2"; searching with no match said "add members with the button below". | Tooltip, spoken label, search-specific message. | Fixed |
| M-10 | Low | Edit-member forced a phone number on members who never had one. | Allowed to stay without; validated when present. | Fixed |
| M-11 | Low | Two entries both called "Group Rules"; "1 months". | Renamed the online one "Online Loan Rules"; singular month. | Fixed |
| M-12 | Medium | The group report and member statement said "Total savings 2,700" online and 2,500 offline (online added the social fund, then listed the social fund again on the next line); a member's report said 1,100 against 1,000 on the Members tab. | Savings is **shares only** everywhere (dashboard, Members, reports, online or offline). Tests updated. | Fixed |
| M-13 | **High** | Choosing a digital champion in the app **replaced the sign-in number of the login the group registered with**. The registrant was then told "Invalid phone/email or password" on their own number (reproduced on the emulator). | Server: the champion's number goes onto the existing login only when that login has no usable number of its own; otherwise the champion gets a login of their own on the same group. Registrant's number and password keep working. Test added. | Fixed |
| M-14 | Medium | Cycles screen: "Cycle 1 · 0 meetings · 7 entries" for a group that held a meeting. Meetings made through the API had **no cycle**, so closing a cycle archived none of them and the "closed cycle refuses new money" rule never applied to them. | New meetings are stamped with the active cycle; a migration stamps existing ones (and creates the cycle row for groups that have none); closing a cycle now seals meetings a phone held (they are never sealed on the server) and rolls merely-planned ones into the next cycle instead of being blocked by them. Migration verified on the QA data (47 meetings, 9 without a cycle row, idempotent, no cross-group stamps). Tests added. | Fixed |
| M-15 | Medium | Welfare said "No meeting is open" beside a meeting that was open on the phone: the server only accepts welfare in an IN_PROGRESS meeting and phone meetings never reach the server until they close. | Server accepts a meeting scheduled within 36 h (the phone's twin); the phone creates that twin (idempotent, the same meeting sync later reuses). Tests added. | Fixed |
| M-16 | Medium | Agent's rating screen: "Meetings completed 0/6 — 0 of 5 meetings sealed" for a group that recorded money in all five, so it was priced as Band D (50 % deposit, highest interest). | A meeting with anything recorded in it counts as completed. Test added. | Fixed |
| M-17 | Low | Member passbook: first load right after sign-in showed "Couldn't load your records" with no button (pull-down recovered it). | "Try again" button. | Fixed |
| M-18 | Medium | **"Back Up This Group" on an agent's Cloud Account opened a completely black screen** (no title, no button; only the system Back key recovered it). | An agent, partner or individual member keeps no group on the phone; the screen returned an empty widget. The entry is now hidden for such accounts and the screen itself shows an explanation with a title bar if it is ever reached. | Fixed |
| M-19 | Low | Caseload report: groups the server had not rated showed **"Band UNRATED · 52"** (a raw enum and a meaningless score) while the list on the home screen said "Unrated". | Only a rated group gets a band chip; others read "No rating yet". | Fixed |
| M-20 | Low | Group business form: the three number fields sat edge to edge, each floating label overlapping the border of the field above (also reach dropdown / buyers). | 16 px gaps. | Fixed |
| M-21 | Low | Amounts on the business card and the Group-rules summary were rounded to whole shillings and had no thousands separator ("KSh 45000"; a share of 50.50 read "KSh 51"; an interest rate of 2.5 % read "3 %"; a 1.5× borrowing limit read "2×"). | Uses the shared formatter (keeps real cents, groups thousands) and prints rates exactly. | Fixed |
| M-22 | Low | Agent's group screen headed all-time ledger totals "Where the money stands"; after a share-out the savings figure still shows what was put in over the years while the group holds nothing. | Re-headed "Totals since the group began" (the figures themselves are correct lifetime totals). | Fixed |
| M-23 | **High** | **After a share-out the phone carried on as if nothing had been paid out.** Reproduced on the emulator: Achieng was paid her 1,000 and the group moved to Cycle 2, yet the dashboard still read "Total Savings 1,500", the Members tab "Savings KSh 1,000", the next meeting would open with the old cycle's money "in the box", and the loan screens would still lend from a fund that no longer held it and let members borrow three times savings that had been paid out. | Share-out is scoped to the cycle, but every balance query (dashboard, members, cash box, loan fund, borrowing limit) summed the whole history and never subtracted the payouts. | Cash box and loan fund now subtract what was paid out (the whole entitlement; a welfare fund the group kept stays in the box), and the dashboard, members and borrowing limit count only the current cycle - the same rule the next share-out uses. New test `share_out_repository_test` ("balances start again"). | Fixed |
| M-24 | Low | Signing out from More -> Cloud Account left that screen (and Account) stacked on top of the sign-in choice, saying "Not connected", with a Sign-out button that did nothing; an agent was also told about "your group's savings". | Same wording and same ending as Account -> Sign out (agent note, straight back to the sign-in choice). | Fixed |
| M-25 | Medium | If the **server** is down for a while but the phone keeps its own signal, nothing prompted a retry: the phone only retries on a signal change or every ten minutes. A treasurer closing a meeting during a deploy saw "1 pending" for up to ten minutes. (Verified the other way: switching the phone's network off and on synced all 3 pending entries within 10 s, exactly once each.) | After a run that leaves changes waiting, retry after 1, 3 then 7 minutes (then the ten-minute timer carries on). Tests added; the stale "notify only when something synced" test was updated to the documented behaviour. | Fixed |
| M-26 | Low | Five user-visible strings written during this audit were hard-coded English; the project's translation-coverage test failed. | Moved to the language files (new keys in all five languages; Swahili translated, the three unreviewed languages carry the English text until a speaker checks them). | Fixed |
| M-27 | **High** | **A meeting that had been given a server "twin" while open was never sent once closed** (a regression from fix M-15, caught on the emulator): after opening Welfare in meeting #2 and closing it, the phone said "synced", the server held none of its shares, loan or repayment, and nothing would ever retry. | The twin was recorded in the same map the sync reads as "this meeting has been pushed". | The twin is recorded separately; a meeting is marked pushed only when a full pass has run (a pass that stops part-way is retried, as is one with conflicts). Two regression tests. The rest of the sweep found no other path that maps early. | Fixed |
| M-28 | Medium | Closing a meeting online did not send it: the phone waited for a signal change, the ten-minute timer or a manual push. | The close now triggers a push straight away (a no-op offline or when the group is not linked; a failure stays on the pending badge and retries). | Fixed |
| M-29 | Low | An error such as "Loan fund holds only KSh 1,500 - short by KSh 500" or "More than the outstanding balance" stayed on screen after the amount had been corrected (loan, repayment, fine, external loan, store order, vote, agreed action, sign-in and set-up forms). | Those forms re-check as the person types. | Fixed |
| M-30 | **High** | **Anyone holding the phone could open a group's record book by creating a new group account.** After group A signed out, registering "QA Onboard Group" (free, needs only a network) landed on group A's dashboard - its 9 members, savings and loan - because the book on the phone was tied to nobody; role checks already kept members and agents out, but not a second group account. | Routing looked only at "is there a book on this phone". | The account remembers its server group; the book knows which server group it is linked to; a group account whose group differs from the linked one gets a "This phone holds another group's records" screen whose only action is Sign out. A book that was never linked, or an account whose group is not known yet (older session, offline), opens as before so nobody is locked out on a guess. Routing tests added (5); translated (Swahili). | Fixed |
| M-31 | Medium | Set-up wizard: pressing Next with no founding member showed "Add at least one founding member." in a snack bar that appeared **behind the Next button** - unreadable, so the press seemed to do nothing (the duplicate-name message had the same problem). | Both messages are shown inline under the member field. | Fixed |
| M-32 | Low | A group created in the wizard, or a member added afterwards, reached the server only at the next reconnect or ten-minute timer (the console showed a group with no members for a while). | Both trigger a sync at once. Verified: after Create Group the founding members were on the server. | Fixed |

### H-2b. Field-agent and group flows exercised in the last pass

Field agent (demo agent, 12 groups): My Groups list and counters - caseload report (no more "Band UNRATED"), Share Report sheet - group detail (credit rating breakdown, all-time totals, members, meetings) - **Record a visit**: visit type, **location capture**, 46-question scorecard over 7 sections (Yes / Partial / No, live score 38 of 92 "Fair", 17 unanswered), photo evidence (permission prompt, camera, add / remove), mentorship (topic, advice text with counter, four 1-5 ratings from the group), agreed action (owner, date picker, done / reopen), group business form (validation, every field, save), notes, **Finish** - Account (contact details, language, appearance, change password with empty / short / mismatched / wrong-current cases, show-hide, forgot-password entry) - Cloud Account (Cloud Data with group switcher, sign-out).
Server check of that visit: type, notes, location (-0.53, 37.45, +/-5 m, outcome `NO_GROUP_LOCATION`), mentorship + 4 ratings, agreed action (due 15 Oct), business (KSh 45,000 / 30,000), and - after fix QA-013 - the scorecard (38 / 92 = 41.3 %, complete = false, 29 answers). The console's visit page shows the same figures.

Group account (QA group A): sign-in, load group, meeting security, meeting 1 (attendance, shares, fine, close and lock), **offline** meeting recording, meeting 2 with welfare payment (server twin), loan disbursement (over-fund guard "short by 500"), repayment (over-payment guard), close, meeting 3, **share-out** (preview, welfare split, confirm, past share-outs), balances afterwards (fixed, M-23), Members and member detail / report, Group settings wizard (all four steps, saved), Invite people (link, copy, rotate confirmation), Requests to join (Waiting / Answered), Group report, Saving cycles, Back up to cloud (push is idempotent), sign-out and back in (records kept).

Onboarding (final pass, clean install): Create Account -> Our Group (empty, short and mismatched-password validation, the mismatch message clearing live) -> sign in -> **Set up my group on this phone** wizard (all four steps, live validation, inline founding-member and duplicate-name messages, Create Group) -> the founding members reached the server 6 s later, without duplicating members already there; then, on a phone holding another group's book, a second group account was stopped at "This phone holds another group's records" (M-30) and the legitimate account opened it normally. Member account: passbook, My savings across all groups, My report (Share / Download).

### H-2c. Connectivity and recovery ("does it come back if it goes off?")

The request "check if billing gets back in case it goes off" was interpreted as *connectivity and syncing*: the platform has no subscription or billing gate for groups (the only money features are the partner wallet and gateway payments, which record every attempt and hold no "switched-off" latch). What was tested, on the emulator against the QA server:

| Scenario | Result |
|---|---|
| Server stopped; phone keeps working (share purchases, a fine, close meeting) | Everything recorded and readable; header shows "1 pending" - no crash, no data loss |
| Phone network switched off then on | All 3 pending entries reached the server within 10 s, **exactly once each** (member, amount, type correct); "pending" cleared |
| Server back but phone never lost signal | Nothing prompted a retry for up to 10 minutes (M-25) - now retries after 1, 3 and 7 minutes |
| Closing a meeting while online | Was not sent until the next trigger (M-28) - now sent at once; meeting 3 reached the server 4 s after closing |
| Meeting given a server twin while open, then closed | Was never sent and showed "synced" (M-27, a regression from M-15) - fixed and tested |
| "Push Closed Meetings" pressed repeatedly | "Synced N records" each time, ledger unchanged (idempotent) |
| Sign out then sign in | Local records intact; the phone number is remembered |
| Wrong password / forged session | Friendly message, no lock-out state left behind |

### H-2d. Web console (Next.js) - desktop and phone-width

Signed in as the platform admin against the QA API; every dashboard route was opened, layout heuristics run (horizontal overflow, squeezed controls, broken images, "undefined / NaN / [object Object]"), and the key group, visit, ledger, welfare, members, cycles and scorecard pages were read against the database.

| ID | Sev | Finding | Fix |
|---|---|---|---|
| W-1 | Medium | On a phone-width screen every button under a group's title (Edit, Meetings, Members, Ledger ...) was a 170 px-tall slab in one column, and the title floated 90 px in from them. A `flex: 1 1 136px` meant for a row was applied to a column. | Buttons flow in rows; heading left-aligned. |
| W-2 | Medium | Field-visit page: the action plan's "Mark done" / "Drop" buttons broke one letter per line (45 px wide, 100 px tall). | `white-space: nowrap` on buttons in table action cells. |
| W-3 | Medium | Welfare page said "No meeting is open" beside a live phone-kept meeting (it accepted only IN_PROGRESS while the server accepts a SCHEDULED meeting within 36 h), and its form was unstyled browser controls. | Same rule as the server; form uses the console's form styling. Verified: payment recorded, balance 150 -> 130. |
| W-4 | Low | Saving-cycles card text sat flush against the card edge; "1 entries", "1 clusters". | Padding; singular forms on ledger, group, cycle, report and map summaries. |
| W-5 | Low | A rejected form (HTTP 400, e.g. "Enter a valid phone number.") was logged as `console.error` and raised Next's red "1 Issue" badge for behaviour that was working. | 4xx logged as warnings; 5xx / network stay errors. |

Also confirmed: wrong-password message ("...do not match an account ... or sign in with a code sent by SMS"); add-member validation on the console ("Enter a valid phone number."), valid add ("Web Test Member added. Default meeting PIN queued for SMS."); members created on the phone with no number appear on the console as "Needs PIN"; ledger page totals (credits 7,450 - debits 600 = 6,850) equal the funds; the visit page shows the phone's scorecard (41.3 %, 17 unanswered warning) and mentorship; 172 web unit tests and the type-check pass.

### H-3. Observations (no change)

* The "synced" chip does not show an offline state; the Unlock screen and the Voting/Welfare screens do say so.
* An absent member can still be picked for shares.
* The phone quotes a loan's total due at the end of its term (principal + interest) while the console accrues interest per whole 30-day month; totals agree at term, differ during it.
* Choosing a theme returns the app to the Dashboard instead of staying on the Appearance screen.
* While testing, the host machine was starved of CPU by unrelated desktop apps (a live-wallpaper service and a voice-changer, ~600 % CPU); the emulator hit "System UI isn't responding" ANRs. These are not app faults, but note that location capture and other main-thread work will feel slow on very weak or overloaded phones.
* Debug builds show the session token in Cloud Account → Access key; the field is not present in release builds (`kReleaseMode` gate).

## I. Remaining issues and recommendations

### Needs a decision or a later change (not fixed)

| # | Sev | Issue | Recommendation |
|---|---|---|---|
| R-1 | **High** | **A share-out done on the phone never reaches the server.** The phone's own books are correct afterwards (M-23) but the payout rows go to a queue that nothing drains; the console keeps saying "Cycle 1", its funds still hold the money, member statements from the server still show the old savings, and "Sync & Backup" says "Everything is backed up". The share-out confirmation now warns about this in plain words. | Push each member's payout as idempotent ledger entries derived from the local payout id (loan settlement as `LOAN_REPAYMENT`, the entitlement as `SHARE_OUT_PAYOUT`, welfare as `WELFARE_SHARE_OUT`) into a "share-out" meeting twin, only once every closed meeting has synced, then close the server cycle; show a failure in Sync & Backup. Deliberately **not** done unattended: the ledger is append-only and the console also offers a server-side reviewed share-out, so using both would pay members twice. Needs a product decision on which one is authoritative. |
| R-2 | Medium | **"Load my group" restores the roster and settings only, not the history.** A treasurer who changes phone starts with zero balances locally while the server holds the record; loan rules come back as defaults (10 % flat, 3x, 1 month) until synced. | Import ledger, meetings and loans from the server for a restored group, or have such a phone read balances from the server. |
| R-6 | Medium | **An unlinked local book claims the first group account that signs in.** The auto-link rule (one book on the phone, not yet linked, and the server group has no records yet) cannot tell the treasurer registering their own group after building the book offline from someone else registering a different group on the same handset - and would then send the book's records into that group. M-30 stops the opposite case (a book already linked to another group). | Ask the person to confirm the group's name before the first link, or link only when the names match. |
| R-7 | Low | The set-up wizard's loan defaults are **5 % reducing balance, 2x, 3 months**, while the platform's decided model (and the defaults "Load my group" uses) is flat monthly interest on the original principal, 10 %, 3x, 1 month. A new group that keeps the defaults will see different loan totals on the phone and in the console (the phone only tells the server its rate when it is flat). | Default the wizard to the platform model (flat, 10 %, 3x, 1 month) and always send the group's loan rules when it is linked. Not changed unattended: it alters what new groups start with. |
| R-3 | Low | The unreviewed languages (Gikuyu, Dholuo, Kiembu) show English for this build's new strings. | Native-speaker pass (docs/translation). |
| R-4 | Low | Observations in H-3 (no offline state on the "synced" chip, absent members can buy shares, theme change resets navigation, loan total-due wording during the term). | Product call. |
| R-5 | Info | The console's Google map tiles did not load in the QA sandbox (broken image in the map); not investigated as it depends on the external tile service and key. | Check on the live site. |

### Operational, for the owner

* **Deploy.** The server and console fixes (QA-001 ... QA-013, W-1 ... W-5, one migration) are committed on the branch `feat/qa-full-audit-2026-09` and pushed for CI; production deploys on a push to `main`, which was **not** done unattended. The migration `20260920100000_stamp_meetings_with_cycle` only adds cycle rows and stamps meetings that have none; it was run on a copy of the QA data (47 meetings, 9 groups without a cycle row, idempotent, no cross-group stamps). **QA-013 (scorecards never arriving) is fixed for phones already in the field the moment the server is updated.**
* **Phone.** `RELEASE_NOTES_2.6.1.md` describes build 22; the app bundle was rebuilt from a clean tree with the production address (the release build refuses a development address, which is the guard working). Upload to Play is left to the owner.
* **Not touched.** The group-access export workflow (`export-group-access.yml`, `export-group-access.ts`) and the manual-generation scripts are still uncommitted files from earlier work; SMS and payment gateways were not exercised (credentials belong in GitHub secrets).
* **Not tested.** A physical handset (only an x86_64 emulator; a debug build froze once under host overload, a profile build did not), iOS, and live payment / SMS gateways.

### If this were a release gate

Ship the server and console update first (it carries QA-013 and the ledger / loan-position fixes), then the phone build. Do not let a group run its first phone share-out before R-1 is decided, or tell them the payouts live on the phone only.
